AWSTemplateFormatVersion: "2010-09-09"
Description: Security Group do Application Load Balancer (Metabase)

Parameters:
  VpcId:
    Type: AWS::EC2::VPC::Id
    Description: VPC onde o ALB sera criado

  Environment:
    Type: String
    Default: prod
    AllowedValues:
      - prod
      - hml
    Description: Ambiente da stack

Resources:

  ALBSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Security Group do ALB. Recebe trafego HTTP/HTTPS da internet e encaminha para o ECS.
      VpcId: !Ref VpcId

      # 🔽 ENTRADA — Internet → ALB
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 80
          ToPort: 80
          CidrIp: 0.0.0.0/0
          Description: HTTP da internet

        - IpProtocol: tcp
          FromPort: 443
          ToPort: 443
          CidrIp: 0.0.0.0/0
          Description: HTTPS da internet

      # 🔼 SAÍDA — ALB → VPC
      # (quem controla o acesso é o SG do ECS)
      SecurityGroupEgress:
        - IpProtocol: -1
          CidrIp: 0.0.0.0/0
          Description: ALB para ECS

      Tags:
        - Key: Project
          Value: Metabase
        - Key: Component
          Value: ALB
        - Key: Environment
          Value: !Ref Environment

Outputs:

  ALBSecurityGroupId:
    Description: Security Group do ALB
    Value: !Ref ALBSecurityGroup
    Export:
      Name: !Sub Metabase-ALB-SG-${Environment}
